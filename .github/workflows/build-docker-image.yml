name: build-docker-image

on:
  workflow_call:
    inputs:
      suffix:
        required: false
        type: string

env:
  CONTAINER_REGISTRY: ghcr.io
  BUILDX_NO_DEFAULT_ATTESTATIONS: 1

jobs:
  build-docker-image:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        # no point in more than base image supports
        # ignore 32-bit - 386, arm/v5, arm/v7
        # arm64 == arm64/v8
        platform: [amd64]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true
          submodules: true

      - name: Prepare
        run: |
          platform=${{ matrix.platform }}
          echo "PLATFORM_PAIR=${platform//\//}" >> $GITHUB_ENV
          echo "GITHUB_REPOSITORY_LOWER=${GITHUB_REPOSITORY@L}" >> $GITHUB_ENV
          echo "TARGET_ROOT=${{ case( startsWith(github.ref, 'refs/tags/v') || contains('["main", "dev", "unstable"]', github.ref_name), 'firestarr', 'cache' ) }}" >> $GITHUB_ENV


      - run: |
          echo "REGISTRY=${CONTAINER_REGISTRY}/${GITHUB_REPOSITORY_LOWER}" >> $GITHUB_ENV
          echo "SUFFIX_ONLY=${{ env.SUFFIX }}" >> $GITHUB_ENV
          echo "SUFFIX=${{ case ( '' != env.SUFFIX, '-', '' ) }}${{ env.SUFFIX }}" >> $GITHUB_ENV

      - run: |
          echo "TARGET_CACHE=${REGISTRY}/cache" >> $GITHUB_ENV
          echo "TARGET=${REGISTRY}/${TARGET_ROOT}" >> $GITHUB_ENV
          echo "IMAGE=firestarr${{ env.SUFFIX }}" >> $GITHUB_ENV

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.TARGET }}

      - name: Login to container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.CONTAINER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: .docker/Dockerfile
          target: ${{ inputs.image }}
          platforms: linux/${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          tags: ${{ env.TARGET }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          # ${{ github.repository }} isn't lowercase so manually specify
          cache-from: |
            type=registry,ref=${{ env.TARGET_CACHE }}
          cache-to: |
            type=registry,mode=max,ref=${{ env.TARGET_CACHE }}

      - name: Export digest
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ inputs.image }}-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    needs: [ build-docker-image ]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true
          submodules: true

      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-${{ inputs.image }}-*
          merge-multiple: true

      - name: Login to container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.CONTAINER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.TARGET }}
          flavor: |
            latest=${{ case ( '' == env.SUFFIX && startsWith(github.ref, 'refs/tags/v'), 'auto', 'false' ) }}
            prefix=
            suffix=${{ env.SUFFIX }}
          tags: |
            type=ref,event=pr
            type=ref,prefix=,suffix=,event=branch,enable=${{ !startsWith(github.ref, 'refs/tags/v') && '' == env.SUFFIX }}
            type=ref,prefix=,suffix=-latest,event=branch,enable=${{ !startsWith(github.ref, 'refs/tags/v') && '' == env.SUFFIX }}
            type=ref,prefix=,suffix=${{ env.SUFFIX }},event=branch,enable=${{ !startsWith(github.ref, 'refs/tags/v')  && '' != env.SUFFIX }}
            type=ref,prefix=,suffix=${{ env.SUFFIX }}-latest,event=branch,enable=${{ !startsWith(github.ref, 'refs/tags/v')  && '' != env.SUFFIX }}
            type=semver,pattern={{version}},prefix=,suffix=,enable=${{ startsWith(github.ref, 'refs/tags/v')  && '' == env.SUFFIX }}
            type=semver,pattern={{version}},prefix=,suffix=${{ env.SUFFIX }},enable=${{ startsWith(github.ref, 'refs/tags/v')  && '' != env.SUFFIX }}
            type=semver,pattern={{major}}.{{minor}},prefix=,suffix=,enable=${{ startsWith(github.ref, 'refs/tags/v')  && '' == env.SUFFIX }}
            type=semver,pattern={{major}}.{{minor}},prefix=,suffix=${{ env.SUFFIX }},enable=${{ startsWith(github.ref, 'refs/tags/v')  && '' != env.SUFFIX }}
            type=raw,value=${{ env.SUFFIX_ONLY }},prefix=,suffix=,enable=${{ startsWith(github.ref, 'refs/tags/v') && '' != env.SUFFIX }}
            type=raw,value=${{ env.SUFFIX_ONLY }}-latest,prefix=,suffix=,enable=${{ startsWith(github.ref, 'refs/tags/v') && '' != env.SUFFIX }}

      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ env.TARGET }}@sha256:%s ' *)

      - name: Inspect image
        run: |
          docker buildx imagetools inspect ${{ env.TARGET }}:${{ steps.meta.outputs.version }}
